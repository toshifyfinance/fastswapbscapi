{
  "name": "fastswapp",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ polaris-labs/fastswapp",
    "create-local": "graph create --node http://localhost:8020/ polaris-labs/fastswapp",
    "remove-local": "graph remove --node http://localhost:8020/ polaris-labs/fastswapp",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 polaris-labs/fastswapp"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
